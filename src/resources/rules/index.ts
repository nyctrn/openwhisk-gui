import RuleList from "./RuleList";
import RuleCreateEdit from "./RuleCreateEdit";

const Rules = {
  name: "rules",
  list: RuleList,
  create: RuleCreateEdit,
  edit: RuleCreateEdit,
};

export default Rules;

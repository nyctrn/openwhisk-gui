import PackageList from "./PackageList";
import PackageCreateEdit from "./PackageCreateEdit";

const packages = {
  name: "packages",
  list: PackageList,
  create: PackageCreateEdit,
  edit: PackageCreateEdit,
};

export default packages;

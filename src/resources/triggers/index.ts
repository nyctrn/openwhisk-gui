import TriggerList from "./TriggerList";
import TriggerCreateEdit from "./TriggerCreateEdit";

const triggers = {
  name: "triggers",
  list: TriggerList,
  edit: TriggerCreateEdit,
  create: TriggerCreateEdit,
};

export default triggers;
